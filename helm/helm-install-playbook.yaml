---
- name: "Install Helm on Bastion hosts"
  hosts: "bastion,k8s_bastion,ocp_bastion"
  gather_facts: "true"

  tasks:
    - name: "Install Helm"
      ansible.builtin.package:
        name: helm
        state: present
      become: true
      register: helm_install_res

    - name: "Install Helm result"
      ansible.builtin.debug:
        var: helm_install_res
        verbosity: 2
...
